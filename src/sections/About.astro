---
// Importar el componente Paragraph
import Paragraph from "../components/Paragraph.astro";
import StackCard from "../components/StackCard.astro";
import { stack } from "../data/stack";

// Aquí usas el contenido con etiquetas HTML directamente
const paragraph = [
  {
    content: `👨‍💻 Como <span style="font-weight: 600;">ingeniero en sistemas</span> especializado en <span style="font-weight: 600;">desarrollo web</span>, estoy entusiasmado por la oportunidad de aportar a su organización mi experiencia en la creación de <span style="font-weight: 600;">📈aplicaciones dinámicas y escalables</span>. He trabajado en diversos proyectos reales, aplicando tecnologías como <span style="font-weight: 600;">React, HTML, CSS</span> y utilizando metodologías ágiles como <span style="font-weight: 600;">Scrum</span> para asegurar entregas eficientes y colaborativas📊.`,
  },
  {
    content: `⚙️ Aunque tengo algunas herramientas preferidas, siempre elijo la mejor para cada tarea, incluso si no está en mi lista habitual. Mi objetivo es proporcionar la <span style="font-weight: 600;"> 🏆 solución adecuada</span> para cada proyecto, enfocándome en la <span style="font-weight: 600;">experiencia del usuario, la eficiencia y el diseño limpio 🔎.</span>`,
  },
  {
    content: `🎵 Más allá de la programación, soy un entusiasta del <span style="font-weight: 600;">🎨 diseño y la ilustración</span>, para mí, son una forma de comunicar de manera visual, capturando emociones e ideas. Un pasatiempo que disfruto mucho es <span style="font-weight: 600;">🎸 tocar la guitarra</span>, ya que me permite relajarme a través de la música.`,
  },
];
---

<section
  id='about'
  class='grid w-full max-w-screen-2xl gap-2 my-2 md:pt-2 grid-cols-1 grid-rows-1 md:grid-cols-5 md:grid-rows-1'
>
  <!-- Sección de tecnologías y herramientas -->
  <div
    class='relative rounded-3xl p-5 md:p-8 col-span-2 md:col-span-2 row-span-1 bg-stone-900 text-white shadow-md flex flex-col justify-center items-center'
  >
    <!-- Fondo con opacidad -->
    <div
      class='absolute inset-0 bg-gradient-to-r from-cyan-500 to-indigo-700 opacity-40 rounded-3xl pointer-events-none'
    >
    </div>

    <!-- Contenido que no será afectado por la opacidad -->
    <div class='relative z-10'>
      <h1 class='text-3xl md:text-5xl font-medium mb-4 text-white text-center'>
        Tecnologías y <span class='text-gray-200'>herramientas</span>
      </h1>

      <!-- Contenedor de la cuadrícula de tecnologías -->
      <div class='grid grid-cols-4 md:grid-cols-5 gap-2 py-3'>
        {stack.map(({ name, icon }) => <StackCard name={name} icon={icon} />)}
      </div>
    </div>
  </div>

  <!-- Sección de sobre mí -->
  <div
    class='rounded-3xl p-5 md:p-8 col-span-2 md:col-span-3 row-span-1 bg-stone-900 shadow-md flex flex-col justify-center items-start text-start'
  >
    <h1 class='text-5xl md:text-6xl font-semibold mb-4 text-white'>Sobre mí</h1>

    {paragraph.map(({ content }) => <Paragraph content={content} />)}
  </div>
</section>
