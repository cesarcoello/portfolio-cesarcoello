---
export interface Props {
  title: string;
  description: string;
  img: string;
  href: string;
}

const { title, description, img, href } = Astro.props;
---

<div class='relative rounded-3xl col-span-5 md:col-span-1 bg-black shadow-md'>
  <!-- Imagen de fondo con opacidad -->
  <div
    class='absolute inset-0 bg-cover bg-center bg-opacity-50 rounded-3xl'
    style={`background-image: url(${img}); opacity: 0.4`}
  >
  </div>

  <!-- Contenido del proyecto -->
  <div class='relative p-5 text-white flex flex-col justify-between h-full'>
    <!-- Título y Descripción -->
    <div>
      <div>
        <h1 class='text-xl md:text-5xl font-semibold mb-2 shadow-sm'>
          {title}
        </h1>
        <p class='text-lg md:text-lg mb-2 shadow-sm'>
          {description}
        </p>
      </div>
    </div>

    <!-- Botón de flecha -->
    <div>
      <a href={href} class='flex justify-end items-end'>
        <button
          class='bg-white hover:bg-transparent text-stone-900 w:h-12 w:w-12 p-2 rounded-full flex justify-center items-center'
        >
          <svg
            class='hover:text-white hover:font-black'
            xmlns='http://www.w3.org/2000/svg'
            width='3em'
            height='3em'
            viewBox='0 0 24 24'
          >
            <path
              fill='currentColor'
              d='M17.6 18L8 8.4V17H6V5h12v2H9.4l9.6 9.6z'></path>
          </svg>
        </button>
      </a>
    </div>
  </div>
</div>
